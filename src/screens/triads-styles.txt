/* Triads Screen Styles */
.triads-screen .controls-panel {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin: 10px 0;
}

.triads-screen .control-group {
  display: flex;
  align-items: center;
}

.triads-screen .control-group label {
  width: 70px;
  font-size: 14px;
  color: var(--text-muted);
}

.triads-screen .scroll-x {
  overflow-x: auto;
  white-space: nowrap;
  -webkit-overflow-scrolling: touch;
  padding-bottom: 4px;
}

.triads-screen .scroll-x::-webkit-scrollbar {
  height: 4px;
}
.triads-screen .scroll-x::-webkit-scrollbar-thumb {
  background: var(--bg-hover);
  border-radius: 4px;
}

.triads-screen .picker {
  display: flex;
  gap: 8px;
}

.triads-screen .picker-btn {
  background: var(--bg-secondary);
  border: 1px solid var(--border);
  color: var(--text);
  padding: 8px 12px;
  border-radius: 8px;
  font-size: 14px;
  cursor: pointer;
  white-space: nowrap;
}

.triads-screen .picker-btn.active {
  background: var(--accent);
  color: #000;
  border-color: var(--accent);
}

.triads-screen .legend {
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-top: 15px;
  font-size: 14px;
  color: var(--text-muted);
}

.triads-screen .legend-item {
  display: flex;
  align-items: center;
  gap: 6px;
}

.triads-screen .dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  display: inline-block;
}

.triads-screen .root-color { background: #ffd700; }
.triads-screen .third-color { background: #4dabf7; }
.triads-screen .fifth-color { background: #69db7c; }
